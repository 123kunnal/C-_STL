#include<iostream>
using namespace std;
typedef long long ll;
// typedef SQ(a) a*a; cannot define fns. its just a renaming tool fo data types
// typedef is a C++ language statement, so it follows the rules of C++ syntax → needs a ;.
// #define is a preprocessor directive, not part of real C++ syntax → it only replaces text → no ;.
// READ PAGE 19 IN BOOK 
int main(){
    ll z=1234567898;
    cout<<SQ(3+3);
    cout<<z;
    return 0;
}